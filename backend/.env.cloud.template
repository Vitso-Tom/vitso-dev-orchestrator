# Vitso Dev Orchestrator - Cloud Configuration Template
# Copy this file based on your deployment target

# ============================================================================
# DEPLOYMENT TYPE
# ============================================================================
CLOUD_PROVIDER=local  # Options: local, aws, azure, gcp, digitalocean

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# For local development (uses Docker PostgreSQL):
DATABASE_URL=postgresql://vitso:vitso_dev_pass@postgres/vitso_dev_orchestrator

# For AWS RDS:
# DATABASE_URL=postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/vitso_dev_orchestrator

# For Azure Database for PostgreSQL:
# DATABASE_URL=postgresql://username@servername:password@servername.postgres.database.azure.com:5432/vitso_dev_orchestrator

# For managed PostgreSQL (DigitalOcean, etc.):
# DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================
# For local development (uses Docker Redis):
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# For AWS ElastiCache:
# REDIS_HOST=your-elasticache-endpoint.cache.amazonaws.com
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# For Azure Cache for Redis:
# REDIS_HOST=your-cache.redis.cache.windows.net
# REDIS_PORT=6380
# REDIS_PASSWORD=your_redis_key

# For managed Redis:
# REDIS_HOST=your-redis-host.com
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# ============================================================================
# AI PROVIDER API KEYS (Required)
# ============================================================================
ANTHROPIC_API_KEY=your_anthropic_key_here
OPENAI_API_KEY=your_openai_key_here
GOOGLE_API_KEY=your_google_key_here

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
BACKEND_PORT=8000
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:8000

# For production, set to your domain:
# VITE_API_URL=https://api.yourdomain.com

# ============================================================================
# DOCKER CONFIGURATION
# ============================================================================
POSTGRES_USER=vitso
POSTGRES_PASSWORD=vitso_dev_pass
POSTGRES_DB=vitso_dev_orchestrator
POSTGRES_PORT=5432

# ============================================================================
# AWS-SPECIFIC (if using AWS)
# ============================================================================
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ACCOUNT_ID=

# ============================================================================
# AZURE-SPECIFIC (if using Azure)
# ============================================================================
AZURE_REGION=eastus
AZURE_SUBSCRIPTION_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# ============================================================================
# SECURITY SETTINGS (Production)
# ============================================================================
# Set to 'true' in production
DEBUG=false
CORS_ORIGINS=http://localhost:3000

# For production with domain:
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ============================================================================
# BACKUP CONFIGURATION
# ============================================================================
# For automated backups
BACKUP_ENABLED=true
BACKUP_S3_BUCKET=  # AWS S3 bucket for backups
BACKUP_AZURE_CONTAINER=  # Azure Blob container for backups
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM (cron format)
